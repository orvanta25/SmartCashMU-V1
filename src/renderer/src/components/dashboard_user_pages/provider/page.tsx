import  AddProvider from '../../../components/dashboard_user/provider/add-provider';

export default function AddProviderPage() {
  return (
    <div className="space-y-6">
      <AddProvider />
    </div>
  );
}