import AddClient from '../../../../components/dashboard_user/client/add-client';

export default function AddClientPage() {
    return (
      <div className="space-y-6">
        <AddClient />
      </div>
    );
  }
